<h2 class="font-xl">
    <%= @products.count%>
</h2>
<%= render 'layouts/search_form', url: products_path, q: @q %>
<main>
    <div class="flex flex-wrap -mx-1 lg:-mx-4">
        <%= render partial: "products/product", collection: @products, as: "product"%>
    </div>
    <%= paginate @products %>
</main>